# 阈值与形态学

```cpp
cv::threshold(gray, binary, 0, 255,
              cv::THRESH_BINARY | cv::THRESH_OTSU);
cv::morphologyEx(binary, clean, cv::MORPH_OPEN, kernel);
```

阈值分割依赖光照；形态学操作依赖结构元素大小。先观察灰度直方图和二值结果，再调整参数。
