# 摄像头与视频循环

```cpp
cv::VideoCapture cap(0);
if (!cap.isOpened()) return 1;

for (;;) {
    cv::Mat frame;
    if (!cap.read(frame) || frame.empty()) break;
    cv::imshow("camera", frame);
    if (cv::waitKey(1) == 27) break; // ESC 退出
}
```

真实设备可能断流、分辨率不支持或编号不同。程序应把这些情况当作正常的输入异常处理。
