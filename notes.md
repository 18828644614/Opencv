# Notes: OpenCV 学习知识库设计

## 结构依据

- 参考 `E:\MarkDown\C-` 的阶段目录、YAML 元数据、README 入口和实践记录方式。
- OpenCV 主线按“环境与 C++ 前置 → 图像基础 → API → 传统视觉 → 实时视频 → 工程化 → 深度学习衔接”组织。

## 学习原则

- 先能读写、观察和保存图像，再学习算法。
- 每个算法都记录输入、输出、参数、失败案例和可视化结果。
- 代码示例默认 C++17、CMake 和 OpenCV 4.x；具体版本以本机安装为准。
- 传统视觉与深度学习不是二选一：OpenCV 负责数据流和工程连接，模型负责复杂识别。

## 几何变换文档补充要点（2026-09-12）

- 统一先讲坐标约定：点写作 `(x, y)`，矩阵访问写作 `at(y, x)`；原点在左上，x 向右、y 向下。
- 解释 API 的核心关系：`resize` 改尺寸，`warpAffine` 使用 2×3 矩阵，`warpPerspective` 使用 3×3 单应矩阵。
- 重点说明插值和边界填充的选择：普通照片优先线性，缩小照片可用 `INTER_AREA`，掩膜/标签必须用最近邻。
- 说明 OpenCV 通过目标像素反查源图像采样；因此变换矩阵、输出尺寸和边界策略必须一起考虑。
- 标注同步：点、框、掩膜分别用同一几何关系变换；框应变换四个角再取外接矩形并裁剪。
- 示例统一采用 C++17、OpenCV 4.x、CMake 和 PowerShell；Windows 路径使用 C++ 原始字符串。
