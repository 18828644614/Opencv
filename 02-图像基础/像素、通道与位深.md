# 像素、通道与位深

一张图像可以看作矩阵：灰度图每个位置一个数，彩色图每个位置通常有 B、G、R 三个数。

- `CV_8UC1`：8 位无符号、单通道，常见灰度图
- `CV_8UC3`：8 位无符号、三通道，常见彩色图
- `CV_32FC1`：32 位浮点、单通道，适合中间计算

不要把“通道数”和“位深”混为一谈。转换到浮点后，数值范围也可能从 `[0, 255]` 变成 `[0, 1]`，显示前要正确缩放。
