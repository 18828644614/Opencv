# C++ 前置知识

OpenCV 不要求先学完整套 C++，但以下内容必须熟悉：

- 函数、引用、指针和 `const`
- 类、构造函数和命名空间
- `std::vector`、`std::string` 和范围 `for`
- 文件读写与异常处理
- CMake、断点调试和编译器警告

如果对对象生命周期、RAII、智能指针和模板不熟，可在 `E:\MarkDown\C-` 中查阅对应章节。学习 OpenCV 时重点关注“谁拥有数据”和“数据是否被复制”。
