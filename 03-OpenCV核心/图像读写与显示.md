# 图像读写与显示

常用 API：

```cpp
cv::Mat image = cv::imread(path, cv::IMREAD_COLOR);
cv::imwrite("result.png", image);
cv::imshow("result", image);
int key = cv::waitKey(1);
```

`waitKey` 不只是等待按键，也负责处理 HighGUI 窗口事件。实时循环中通常使用 `waitKey(1)`，单张图片查看使用 `waitKey(0)`。
