# 滤波与边缘

- 高斯滤波：平滑噪声，常作为边缘检测前处理
- 中值滤波：对椒盐噪声有效
- 双边滤波：尽量保留边缘的同时平滑区域
- Canny：经典多阶段边缘检测

典型流水线：

```cpp
cv::Mat gray, blur, edges;
cv::cvtColor(image, gray, cv::COLOR_BGR2GRAY);
cv::GaussianBlur(gray, blur, {5, 5}, 1.2);
cv::Canny(blur, edges, 50, 150);
```
